import React from "react";

const logo = "https://cdn.auth0.com/blog/auth0-react-sample/assets/logo.png";

const Hero = () => (
  <div className="text-center hero">
    <h1 className="mb-4">Software security project</h1>
    <p className="lead">
     Please proceed to login so you can access your profile data as well as an api fetch call!
    </p>
  </div>
);

export default Hero;
